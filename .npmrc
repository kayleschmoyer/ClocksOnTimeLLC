# Tell prebuild-install to download better-sqlite3 prebuilt binaries
# compiled for Electron (not the system Node.js version).
# This avoids needing Visual Studio C++ build tools on Windows.
target=29.1.0
target_arch=x64
disturl=https://electronjs.org/headers
runtime=electron
